extern int writeln(string); 
extern string itos(int); 

def main() -> int: 

  while (1):
    std.printf("OMG!! OUR FIRST LOOP!");
  end;

  for (int i := -10; i < 10 + 1; ++i):
    if (i > 5):
      writeln(itos(i););
    end;
  end; 

  return 0; 
end; 
